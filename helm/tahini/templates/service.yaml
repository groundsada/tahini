apiVersion: v1
kind: Service
metadata:
  name: {{ include "tahini.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tahini.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tahini.selectorLabels" . | nindent 4 }}
